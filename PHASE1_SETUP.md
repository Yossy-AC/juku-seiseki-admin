# Phase 1 セットアップ完了

## 完成した項目

✅ `pyproject.toml` - uv 依存管理ファイル
✅ `app/main.py` - FastAPI エントリーポイント
✅ `app/config.py` - 設定管理
✅ `app/database.py` - SQLAlchemy 設定
✅ `app/models/` - 4つの SQLAlchemy モデル（Class, Student, Grade, Attendance）
✅ `app/routers/` - 7つのルーター（pages, students, grades, classes, attendance, upload, auth）
✅ `app/templates/` - テンプレート構造（base, login, admin, dashboard, upload）
✅ `static/css/styles.css` - スタイルシート（HTMX インジケーター対応）
✅ `scripts/import_json.py` - JSON → SQLite 移行スクリプト

---

## セットアップ手順

### 1. 依存パッケージをインストール

```bash
cd c:\GitHub_StudentManager\Student-Manager
uv sync
```

### 2. .env ファイルを作成

```bash
# .env.example をコピーして .env を作成
cp .env.example .env

# 内容を編集（必要に応じて）
# SECRET_KEY=your-secret-key
# ADMIN_PASSWORD=your-password
# DATABASE_URL=sqlite:///./student_manager.db
```

### 3. JSON データを SQLite に移行

```bash
uv run python scripts/import_json.py
```

出力例：
```
============================================================
JSON → SQLite データ移行スクリプト
============================================================

🔧 データベーステーブルを作成中...
  ✓ テーブル作成完了

📚 講座データをインポート中...
  ✓ 高3英語@難関大
  ✓ ...
  完了: 7 件の講座

👥 生徒データをインポート中...
  ✓ 生徒A
  ✓ ...
  完了: 3 件の生徒

📊 成績データをインポート中...
  ✓ 成績ID: g001
  ✓ ...
  完了: 7 件の成績

📋 出席データをインポート中...
  ✓ 出席ID: a001
  ✓ ...
  完了: 6 件の出席記録

============================================================
✅ データ移行が完了しました
============================================================
```

### 4. 開発サーバーを起動

```bash
uv run uvicorn app.main:app --reload --port 8000
```

ターミナルに以下のようなメッセージが出力されます：

```
✓ Database initialized
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete
```

### 5. ブラウザで動作確認

- **メインページ**: `http://localhost:8000` → `/admin` へリダイレクト
- **ログイン画面**: `http://localhost:8000/login`
- **管理画面**: `http://localhost:8000/admin`
- **生徒ダッシュボード**: `http://localhost:8000/dashboard/s001`
- **CSVアップロード**: `http://localhost:8000/upload`
- **ヘルスチェック**: `http://localhost:8000/health`

---

## 動作確認ポイント

1. ✅ サーバーが起動できること
2. ✅ ナビゲーションバーが表示されること
3. ✅ タブ切り替えが HTMX で動作すること（管理画面）
4. ✅ ログイン画面がアクセスできること
5. ✅ 生徒ダッシュボードが表示されること（ただしセクションはまだプレースホルダー）

---

## 次のステップ（Phase 2）

- `services/grade_calculator.py` を実装
- 成績計算ロジック（calculateClassAverage, calculateStudentAverage 等）をサーバー側で実装
- ダッシュボードの各セクション（成績テーブル、比較、アドバイス、出席）を DB から動的に取得

---

## トラブルシューティング

### ImportError が出た場合

```bash
# モジュールパスが正しいか確認
uv run python -c "from app.main import app; print('OK')"
```

### SQLite ファイルが見つからない

```bash
# student_manager.db が作成されているか確認
ls -la *.db

# 新しく作成する場合
rm student_manager.db  # 既存ファイルがあれば削除
uv run python scripts/import_json.py
```

### ポート 8000 が使用中の場合

```bash
# 別のポートで起動
uv run uvicorn app.main:app --reload --port 8001
```

---

## ファイル構造の確認

```
Student-Manager/
├── pyproject.toml          ✅ 作成済み
├── .python-version         ✅ 作成済み
├── .env.example            ✅ 作成済み
├── app/
│   ├── __init__.py         ✅
│   ├── main.py             ✅
│   ├── config.py           ✅
│   ├── database.py         ✅
│   ├── models/             ✅ (4ファイル)
│   ├── routers/            ✅ (7ファイル)
│   ├── services/           ⏳ Phase 2 で実装
│   └── templates/          ✅ (テンプレート構造)
├── static/
│   └── css/styles.css      ✅
├── scripts/
│   └── import_json.py      ✅
└── data/
    ├── students.json       （既存）
    ├── grades.json         （既存）
    ├── classes.json        （既存）
    └── attendance.json     （既存）
```

---

Phase 1 のセットアップは完了です。Phase 2 に進む準備ができました！
